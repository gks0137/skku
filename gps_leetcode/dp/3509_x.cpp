#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int maxProduct(vector<int>& nums, int k, int limit) {
        
    }
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    Solution sol;

x
    vector<int> nums = {0, 2, 3};
    int k = -5, limit = 12;
    cout << sol.maxProduct(nums, k, limit) << "\n";



    return 0;
}